import { VFC } from 'react';
import { Icon } from '@iconify/react';
import airbnbIcon from '@iconify/icons-logos/airbnb';

const Logo: VFC = () => {
  return <Icon icon={airbnbIcon} />;
};

export default Logo;
