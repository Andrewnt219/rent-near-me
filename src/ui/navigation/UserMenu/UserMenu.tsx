type Props = {
  className?: string;
};
const UserMenu = ({ className }: Props) => {
  return (
    <button className={className} tw="">
      UserMenu
    </button>
  );
};

export default UserMenu;
